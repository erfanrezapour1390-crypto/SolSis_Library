<Window x:Class="SolSis.Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SolSis Library" Width="1180" Height="760" MinWidth="900" MinHeight="600"
        Background="{StaticResource Bg}" Foreground="{StaticResource Text}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource Panel}">
            <DockPanel Margin="20">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="SolSis" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Accent}"/>
                    <TextBlock Text="LIBRARY" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Muted}" Margin="2,-2,0,30"/>
                    <Button Content="⌂   Home" HorizontalContentAlignment="Left" Margin="0,0,0,8" Click="Home_Click"/>
                    <Button Content="🎮  All Games" HorizontalContentAlignment="Left" Margin="0,0,0,8" Click="AllGames_Click"/>
                    <Button Content="＋  Add Game" HorizontalContentAlignment="Left" Click="AddGame_Click"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock x:Name="TotalGamesText" Foreground="{StaticResource Muted}"/>
                    <TextBlock x:Name="TotalTimeText" Foreground="{StaticResource Muted}" Margin="0,5,0,0"/>
                    <TextBlock Text="SolSis Library v2" Foreground="#596274" FontSize="11" Margin="0,20,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock x:Name="PageTitle" Text="Your Library" FontSize="30" FontWeight="Bold"/>
                    <TextBlock Text="Your games. Your time. Your library." Foreground="{StaticResource Muted}" Margin="0,5,0,0"/>
                </StackPanel>
                <Button DockPanel.Dock="Right" Content="+  ADD GAME" Background="{StaticResource Accent}" FontWeight="Bold" Click="AddGame_Click"/>
            </DockPanel>

            <Border Grid.Row="1" Background="{StaticResource Panel}" CornerRadius="12" Padding="14" Margin="0,0,0,18">
                <DockPanel>
                    <TextBlock Text="⌕" FontSize="20" VerticalAlignment="Center" Margin="3,0,10,0"/>
                    <TextBox x:Name="SearchBox" Background="Transparent" Foreground="{StaticResource Text}"
                             BorderThickness="0" FontSize="14" VerticalContentAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="GamesPanel" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
